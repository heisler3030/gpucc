<%#- Javascript to populate and enable modal -%>

$(".modal-content").replaceWith("<%= escape_javascript(render(:partial => "completedsetform", object: @completed_set)) %>");
$('#myModal').modal('show');